---
import { Flex } from "@radix-ui/themes";
import { BaseTheme } from "../components/BaseTheme";
import { SectionEducation } from "../components/SectionEducation";
import { SectionIntro } from "../components/SectionIntro";
import { SectionJET } from "../components/SectionJET";
import { SectionJourney } from "../components/SectionJourney";
import { SectionOpenUAS } from "../components/SectionOpenUAS";
import { SectionPaintings } from "../components/SectionPaintings";
import BaseLayout from "../layouts/BaseLayout.astro";
---

<BaseLayout>
  <BaseTheme>
    <Flex
      justify="center"
      minHeight="100vh"
      style={{ background: "var(--mauve-1)" }}
      py={{ initial: "0", lg: "9" }}
    >
      <Flex
        direction="column"
        flexGrow="1"
        maxWidth="80rem"
        style={{
          background: "var(--mauve-3)",
          boxShadow: "var(--shadow-3)",
        }}
      >
        <SectionIntro client:only="react">
          <SectionIntro skeleton slot="fallback" />
        </SectionIntro>
        <!-- <SectionHighlights /> -->
        <SectionJourney />
        <SectionEducation />
        <SectionOpenUAS />
        <SectionJET />
        <SectionPaintings />
        <!-- <SectionProjects /> -->
      </Flex>
    </Flex>
  </BaseTheme>
</BaseLayout>
